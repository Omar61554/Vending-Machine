/*******************************************************************************************************************/
/************** AUTHOR:             ********************************************************************************/
/************** DATE: 12/4/2023     ********************************************************************************/
/************** VERSION: 0.1        ********************************************************************************/
/************** FILE:  UI.cpp       ********************************************************************************/
/*******************************************************************************************************************/

#include "UI.hpp"

//constructor
UI::UI()
{
    //initialize the product to 0
    product = 0;
    //initialize the money to 0
    money = 0;
    //initialize the change to 0
    change = 0;
    //initialize the message to 0
    message = 0;
    //initialize the error to 0
    error = 0;
    //initialize the product quantity to 0
    quantity = 0;
    //initialize the product name to 0
    name = 0;
    //initialize the product expiry date to 0
    expiryDate = 0;
    //initialize the product price to 0
    price = 0;
}

//destructor
UI::~UI()
{
    //initialize the product to 0
    product = 0;
    //initialize the money to 0
    money = 0;
    //initialize the change to 0
    change = 0;
    //initialize the message to 0
    message = 0;
    //initialize the error to 0
    error = 0;
    //initialize the product quantity to 0
    quantity = 0;
    //initialize the product name to 0
    name = 0;
    //initialize the product expiry date to 0
    expiryDate = 0;
    //initialize the product price to 0
    price = 0;
}

//function to display the products to the user
void UI::UI_displayProducts()
{
    //display the products to the user
}

//function to get the product the user wants to buy
void UI::UI_getProduct()
{
    //get the product the user wants to buy
}

//function to get the money from the user

void UI::UI_getMoney()
{
    //get the money from the user
}

//function to return the change to the user

void UI::UI_returnChange()
{
    //return the change to the user
}

//function to display the change to the user

void UI::UI_displayChange()
{
    //display the change to the user
}

//function to display the message to the user

void UI::UI_displayMessage()
{
    //display the message to the user
}

//function to display the error to the user

void UI::UI_displayError()
{
    //display the error to the user
}

//function to display the product price to the user

void UI::UI_displayPrice()
{
    //display the product price to the user
}

//function to display the product quantity to the user

void UI::UI_displayQuantity()
{
    //display the product quantity to the user
}

//function to display the product name to the user

void UI::UI_displayName()
{
    //display the product name to the user
}

//function to display the product expiry date to the user

void UI::UI_displayExpiryDate()
{
    //display the product expiry date to the user
}


